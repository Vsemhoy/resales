import React from 'react'
export default function SectionSpecials({ data, onChange, companyId, draftId }) {
  return <div style={{ padding: '12px', color: '#bfbfbf', fontSize: 12 }}>SectionSpecials — в разработке</div>
}
