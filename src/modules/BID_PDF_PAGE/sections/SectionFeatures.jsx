import React from 'react'
export default function SectionFeatures({ data, onChange, companyId, draftId }) {
  return <div style={{ padding: '12px', color: '#bfbfbf', fontSize: 12 }}>SectionFeatures — в разработке</div>
}
