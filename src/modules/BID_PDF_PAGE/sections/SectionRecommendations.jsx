import React from 'react'
export default function SectionRecommendations({ data, onChange, companyId, draftId }) {
  return <div style={{ padding: '12px', color: '#bfbfbf', fontSize: 12 }}>SectionRecommendations — в разработке</div>
}
