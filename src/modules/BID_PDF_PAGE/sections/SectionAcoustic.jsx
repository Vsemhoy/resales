import React from 'react'
export default function SectionAcoustic({ data, onChange, companyId, draftId }) {
  return <div style={{ padding: '12px', color: '#bfbfbf', fontSize: 12 }}>SectionAcoustic — в разработке</div>
}
